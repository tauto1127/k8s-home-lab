minimum_pre_commit_version: "4.6.2"

repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Detect hardcoded secrets (Gitleaks 8.30.1)
        entry: scripts/pre-commit-gitleaks
        language: system
        pass_filenames: false
        always_run: true
